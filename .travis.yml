language: java
jdk: oraclejdk8

install: true
script: mvn clean install

cache:
  directories:
  - $HOME/.m2
deploy:
  -
    on:
      branch: release
    provider: bintray
    skip_cleanup: true
    file: config/bintray.json
    user: real-mielofon
    key: $BINTRAY_API_KEY